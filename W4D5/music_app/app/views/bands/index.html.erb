<h2>Bands</h2>

<ul>
  <% @bands.each do |band| %>
    <li><a href="<%= band_url(Band.find(band.id)) %>"><%= band.name %></a></li>
    <br>
  <% end %>
</ul>

<a href="<%= new_band_url %>">New band</a>
